export default function CaptchaBox() {
  return (
    <div
      style={{
        padding: "10px",
        borderRadius: "10px",
        border: "1px dashed var(--border)",
        color: "var(--text-muted)",
        fontSize: "0.85rem",
        textAlign: "center",
      }}
    >
      CAPTCHA Placeholder
    </div>
  );
}
